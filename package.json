{
  "name": "simple-blog-mern",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "start": "concurrently \"npm:start --workspace backend\" \"npm:start --workspace frontend\"",
    "backend": "npm:start --workspace backend",
    "frontend": "npm:start --workspace frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
